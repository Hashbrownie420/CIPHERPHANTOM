<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>DroidCraft AI - Android Dev Suite</title>
    <style>
        :root {
            --bg-color: #0f172a;
            --card-bg: #1e293b;
            --primary: #10b981;
            --secondary: #3b82f6;
            --accent: #f59e0b;
            --text-main: #f8fafc;
            --text-dim: #94a3b8;
            --danger: #ef4444;
            --safe-top: env(safe-area-inset-top, 31px);
            --safe-bottom: env(safe-area-inset-bottom, 20px);
            --radius: 16px;
        }

        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            font-family: Helvetica, Arial, sans-serif;
            background-color: #020617;
            color: var(--text-main);
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
            -webkit-touch-callout: none;
            overflow: hidden;
        }

        #app-container {
            width: min(90vw, 1200px);
            height: min(90vh, 873px);
            background-color: var(--bg-color);
            border-radius: 24px;
            display: flex;
            flex-direction: column;
            position: relative;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        header {
            padding: var(--safe-top) 20px 15px 20px;
            background: rgba(30, 41, 59, 0.8);
            backdrop-filter: blur(10px);
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            z-index: 10;
        }

        .status-bar {
            display: flex;
            gap: 10px;
            font-size: 0.8rem;
            color: var(--primary);
        }

        main {
            flex: 1;
            overflow-y: auto;
            padding: 20px;
            padding-bottom: 100px;
            scrollbar-width: none;
        }

        main::-webkit-scrollbar {
            display: none;
        }

        .screen {
            display: none;
            animation: fadeIn 0.3s ease-out;
        }

        .screen.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .card {
            background: var(--card-bg);
            border-radius: var(--radius);
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 15px;
        }

        .nav-bar {
            position: absolute;
            bottom: 0;
            width: 100%;
            background: rgba(30, 41, 59, 0.95);
            backdrop-filter: blur(15px);
            display: flex;
            justify-content: space-around;
            padding: 10px 0 calc(10px + var(--safe-bottom)) 0;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            z-index: 100;
        }

        .nav-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 5px;
            color: var(--text-dim);
            font-size: 0.65rem;
            cursor: pointer;
            transition: all 0.2s;
            flex: 1;
            text-align: center;
        }

        .nav-item.active {
            color: var(--primary);
        }

        .nav-item span {
            font-size: 1.3rem;
        }

        h1, h2, h3 {
            margin: 0 0 15px 0;
            font-weight: 600;
        }

        .btn {
            background: var(--primary);
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 10px;
            font-weight: bold;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: opacity 0.2s;
        }

        .btn:active {
            opacity: 0.7;
        }

        .btn-secondary {
            background: var(--secondary);
        }

        .btn-outline {
            background: transparent;
            border: 1px solid var(--primary);
            color: var(--primary);
        }

        input, textarea, select {
            width: 100%;
            background: #0f172a;
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 12px;
            border-radius: 8px;
            color: white;
            margin-bottom: 15px;
            box-sizing: border-box;
            font-family: monospace;
        }

        .code-block {
            background: #000;
            padding: 15px;
            border-radius: 8px;
            overflow-x: auto;
            font-family: monospace;
            font-size: 0.85rem;
            color: #a5d6ff;
            border-left: 4px solid var(--primary);
            margin: 10px 0;
            white-space: pre-wrap;
            word-break: break-all;
        }

        .badge {
            font-size: 0.65rem;
            padding: 2px 6px;
            border-radius: 4px;
            background: var(--secondary);
            margin-left: 5px;
        }

        .search-container {
            position: sticky;
            top: 0;
            background: var(--bg-color);
            padding: 10px 0;
            z-index: 5;
        }

        .chat-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
            height: 300px;
            overflow-y: auto;
            padding: 10px;
            background: #0a0f1e;
            border-radius: 12px;
            margin-bottom: 15px;
        }

        .msg {
            max-width: 85%;
            padding: 10px 15px;
            border-radius: 15px;
            font-size: 0.9rem;
            line-height: 1.4;
        }

        .msg.ai {
            align-self: flex-start;
            background: var(--card-bg);
            border-bottom-left-radius: 2px;
        }

        .msg.user {
            align-self: flex-end;
            background: var(--secondary);
            border-bottom-right-radius: 2px;
        }

        .file-tree {
            font-family: monospace;
            font-size: 0.9rem;
            color: var(--text-dim);
        }

        .file-item {
            padding: 5px 0;
            cursor: pointer;
        }

        .file-item:hover {
            color: var(--primary);
        }

        .loader {
            width: 20px;
            height: 20px;
            border: 2px solid var(--primary);
            border-bottom-color: transparent;
            border-radius: 50%;
            display: inline-block;
            animation: rotation 1s linear infinite;
        }

        @keyframes rotation {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .modal-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.8);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .modal {
            background: var(--card-bg);
            width: 80%;
            max-height: 70%;
            border-radius: 20px;
            padding: 25px;
            overflow-y: auto;
        }
    </style>
</head>
<body>

<div id="app-container">
    <header>
        <div style="font-weight: bold; font-size: 1.2rem;">DroidCraft <span style="color: var(--primary);">AI</span></div>
        <div class="status-bar">
            <span>📡 Online</span>
            <span>🔋 100%</span>
        </div>
    </header>

    <main id="main-content">
        <!-- Home Screen -->
        <section id="home" class="screen active">
            <div class="card" style="background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);">
                <h2>Willkommen, Developer 🛠️</h2>
                <p style="color: var(--text-dim);">Lokale Android-Entwicklungsumgebung & KI-Modelle bereit.</p>
                <div class="grid" style="margin-top: 20px;">
                    <div class="card" style="margin: 0; text-align: center; padding: 15px;">
                        <div style="font-size: 1.5rem;">📦</div>
                        <div style="font-size: 0.8rem; margin-top: 5px;">Projekte</div>
                        <div id="project-count" style="font-weight: bold; color: var(--primary);">0</div>
                    </div>
                    <div class="card" style="margin: 0; text-align: center; padding: 15px;">
                        <div style="font-size: 1.5rem;">🔍</div>
                        <div style="font-size: 0.8rem; margin-top: 5px;">Analysen</div>
                        <div id="analysis-count" style="font-weight: bold; color: var(--secondary);">0</div>
                    </div>
                </div>
            </div>

            <h3>Schnellzugriff</h3>
            <div class="grid">
                <button class="btn btn-outline" onclick="switchTab('ai-lab')">🤖 KI Chat</button>
                <button class="btn btn-outline" onclick="switchTab('reverse')">🔓 Reverse</button>
                <button class="btn btn-outline" onclick="switchTab('hash')">🔐 Hash Gen</button>
                <button class="btn btn-outline" onclick="switchTab('db')">📚 Code DB</button>
                <button class="btn btn-outline" onclick="switchTab('debug')">🐞 Debugger</button>
            </div>

            <div class="card" style="margin-top: 20px;">
                <h3>System Status</h3>
                <div style="display: flex; flex-direction: column; gap: 10px; font-size: 0.9rem;">
                    <div style="display: flex; justify-content: space-between;">
                        <span>Modell: DroidGen-7B</span>
                        <span style="color: var(--primary);">Geladen</span>
                    </div>
                    <div style="display: flex; justify-content: space-between;">
                        <span>Krypto-Engine:</span>
                        <span style="color: var(--secondary);">SubtleCrypto</span>
                    </div>
                    <div style="display: flex; justify-content: space-between;">
                        <span>Speicher:</span>
                        <span>Lokal (localStorage)</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Code Database Screen -->
        <section id="db" class="screen">
            <div class="search-container">
                <input type="text" id="db-search" placeholder="Suche nach Snippets (z.B. Intent, Room, UI)..." oninput="filterDB()">
            </div>
            <div id="db-list">
                <!-- Snippets will be injected here -->
            </div>
        </section>

        <!-- Reverse Engineering Screen -->
        <section id="reverse" class="screen">
            <div class="card">
                <h3>APK Analyse 🔓</h3>
                <p style="font-size: 0.9rem; color: var(--text-dim);">Simuliere das Decompiling einer APK Datei.</p>
                <input type="text" id="apk-name" placeholder="app_name.apk">
                <button class="btn" onclick="simulateReverse()">Analyse starten</button>
            </div>
            
            <div id="reverse-result" style="display: none;">
                <div class="card">
                    <h4>Struktur: <span id="current-apk-label"></span></h4>
                    <div class="file-tree" id="file-tree-container">
                        <!-- File tree injected here -->
                    </div>
                </div>
                <div id="file-content-viewer" class="code-block" style="display: none;"></div>
            </div>
        </section>

        <!-- Hash Generator Screen -->
        <section id="hash" class="screen">
            <div class="card">
                <h3>Hash Generator 🔐</h3>
                <p style="font-size: 0.9rem; color: var(--text-dim);">Erzeuge kryptografische Hashes für Signaturen oder Datenintegrität.</p>
                
                <label style="font-size: 0.8rem; color: var(--text-dim); display: block; margin-bottom: 5px;">Eingabetext:</label>
                <textarea id="hash-input" rows="4" placeholder="Text oder String hier eingeben..." oninput="generateHash()"></textarea>
                
                <label style="font-size: 0.8rem; color: var(--text-dim); display: block; margin-bottom: 5px;">Algorithmus:</label>
                <select id="hash-algo" onchange="generateHash()">
                    <option value="SHA-256">SHA-256 (Standard)</option>
                    <option value="SHA-1">SHA-1 (Legacy)</option>
                    <option value="SHA-512">SHA-512 (High Security)</option>
                </select>

                <label style="font-size: 0.8rem; color: var(--text-dim); display: block; margin-bottom: 5px;">Resultat:</label>
                <div class="code-block" id="hash-output">...</div>
                
                <button class="btn btn-secondary" style="width: 100%;" onclick="copyHash()">Hash in Zwischenablage kopieren</button>
            </div>
        </section>

        <!-- Debugger Screen -->
        <section id="debug" class="screen">
            <div class="card">
                <h3>Android Debugger 🐞</h3>
                <p style="font-size: 0.9rem; color: var(--text-dim);">Füge deinen Code ein, um ihn auf häufige Fehler zu prüfen.</p>
                <textarea id="debug-input" rows="8" placeholder="Füge hier Java/Kotlin/XML Code ein..."></textarea>
                <button class="btn btn-secondary" onclick="runDebugger()">Code Scannen</button>
            </div>
            <div id="debug-results"></div>
        </section>

        <!-- AI Lab Screen -->
        <section id="ai-lab" class="screen">
            <div class="card" style="padding: 10px;">
                <select id="model-select">
                    <option value="droidgen">DroidGen-7B (General Coding)</option>
                    <option value="smalimaster">Smali-Master (Reverse Engineering)</option>
                    <option value="sec-audit">Security-Audit (Vulnerability Scan)</option>
                </select>
            </div>
            <div class="chat-container" id="chat-box">
                <div class="msg ai">Hallo! Ich bin dein lokaler Android-Experte. Wie kann ich dir heute beim Coden helfen?</div>
            </div>
            <div style="display: flex; gap: 10px;">
                <input type="text" id="chat-input" placeholder="Frage nach Code oder Logik..." onkeypress="if(event.key==='Enter') sendChat()">
                <button class="btn" onclick="sendChat()" style="padding: 12px;">🚀</button>
            </div>
        </section>
    </main>

    <nav class="nav-bar">
        <div class="nav-item active" onclick="switchTab('home')" id="nav-home">
            <span>🏠</span>
            <div>Home</div>
        </div>
        <div class="nav-item" onclick="switchTab('db')" id="nav-db">
            <span>📚</span>
            <div>DB</div>
        </div>
        <div class="nav-item" onclick="switchTab('reverse')" id="nav-reverse">
            <span>🔓</span>
            <div>Rev</div>
        </div>
        <div class="nav-item" onclick="switchTab('hash')" id="nav-hash">
            <span>🔐</span>
            <div>Hash</div>
        </div>
        <div class="nav-item" onclick="switchTab('debug')" id="nav-debug">
            <span>🐞</span>
            <div>Debug</div>
        </div>
        <div class="nav-item" onclick="switchTab('ai-lab')" id="nav-ai-lab">
            <span>🤖</span>
            <div>AI</div>
        </div>
    </nav>
</div>

<div id="timePickerModal" class="modal-overlay">
    <div class="modal">
        <h3>Zeit wählen</h3>
        <div style="display: flex; justify-content: center; gap: 20px; margin: 20px 0;">
            <select id="hourSelect" style="width: 70px;"></select>
            <select id="minSelect" style="width: 70px;"></select>
        </div>
        <button class="btn" onclick="confirmTime()" style="width: 100%;">Bestätigen</button>
    </div>
</div>

<script>
    const APP_STORAGE_KEY = 'DROIDCRAFT_PRO_DATA_V2';
    window.APP_STORAGE_KEY = APP_STORAGE_KEY;

    const CODE_DB = [
        { title: "MainActivity Template", lang: "Kotlin", category: "UI", code: "class MainActivity : AppCompatActivity() {\n    override fun onCreate(savedInstanceState: Bundle?) {\n        super.onCreate(savedInstanceState)\n        setContentView(R.layout.activity_main)\n    }\n}" },
        { title: "Internet Permission", lang: "XML", category: "Manifest", code: "<uses-permission android:name=\"android.permission.INTERNET\" />" },
        { title: "Start Activity Intent", lang: "Kotlin", category: "Intent", code: "val intent = Intent(this, SecondActivity::class.java)\nstartActivity(intent)" },
        { title: "Room Database Setup", lang: "Kotlin", category: "Storage", code: "@Database(entities = [User::class], version = 1)\nabstract class AppDatabase : RoomDatabase() {\n    abstract fun userDao(): UserDao\n}" },
        { title: "Retrofit Instance", lang: "Kotlin", category: "Network", code: "val retrofit = Retrofit.Builder()\n    .baseUrl(\"https://api.example.com/\")\n    .addConverterFactory(GsonConverterFactory.create())\n    .build()" },
        { title: "Custom View Draw", lang: "Java", category: "UI", code: "@Override\nprotected void onDraw(Canvas canvas) {\n    super.onDraw(canvas);\n    canvas.drawCircle(x, y, radius, paint);\n}" },
        { title: "Broadcast Receiver", lang: "Kotlin", category: "System", code: "val br: BroadcastReceiver = MyBroadcastReceiver()\nval filter = IntentFilter(ConnectivityManager.CONNECTIVITY_ACTION)\nregisterReceiver(br, filter)" }
    ];

    const MOCK_FILES = {
        "AndroidManifest.xml": `<?xml version="1.0" encoding="utf-8"?>\n<manifest xmlns:android="http://schemas.android.com/apk/res/android"\n    package="com.example.app">\n    <application>\n        <activity android:name=".MainActivity" android:exported="true">\n            <intent-filter>\n                <action android:name="android.intent.action.MAIN" />\n            </intent-filter>\n        </activity>\n    </application>\n</manifest>`,
        "classes.dex (Smali)": `.class public Lcom/example/app/MainActivity;\n.super Landroidx/appcompat/app/AppCompatActivity;\n\n.method protected onCreate(Landroid/os/Bundle;)V\n    .locals 1\n    invoke-super {p0, p1}, Landroidx/appcompat/app/AppCompatActivity;->onCreate(Landroid/os/Bundle;)V\n    return-void\n.end method`,
        "res/layout/activity_main.xml": `<LinearLayout ...>\n    <TextView android:text="Hello World!" />\n</LinearLayout>`
    };

    let appData = {
        projects: 0,
        analyses: 0,
        chatHistory: []
    };

    function saveData() {
        localStorage.setItem(APP_STORAGE_KEY + '_data', JSON.stringify(appData));
    }

    function loadData() {
        const saved = localStorage.getItem(APP_STORAGE_KEY + '_data');
        if (saved) {
            appData = JSON.parse(saved);
            document.getElementById('project-count').innerText = appData.projects || 0;
            document.getElementById('analysis-count').innerText = appData.analyses || 0;
            renderChatHistory();
        }
    }

    function switchTab(tabId) {
        document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
        document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
        
        document.getElementById(tabId).classList.add('active');
        document.getElementById('nav-' + tabId).classList.add('active');
    }

    function renderDB(filter = '') {
        const list = document.getElementById('db-list');
        list.innerHTML = '';
        CODE_DB.filter(item => 
            item.title.toLowerCase().includes(filter.toLowerCase()) || 
            item.category.toLowerCase().includes(filter.toLowerCase())
        ).forEach(item => {
            const card = document.createElement('div');
            card.className = 'card';
            card.innerHTML = `
                <div style="display:flex; justify-content:space-between; align-items:center;">
                    <h4 style="margin:0;">${item.title} <span class="badge">${item.lang}</span></h4>
                    <span style="font-size:0.7rem; color:var(--primary);">${item.category}</span>
                </div>
                <div class="code-block">${item.code}</div>
                <button class="btn btn-outline" style="font-size:0.7rem; padding:5px 10px;" onclick="copyCode(this)">Kopieren</button>
            `;
            list.appendChild(card);
        });
    }

    function filterDB() {
        const val = document.getElementById('db-search').value;
        renderDB(val);
    }

    function copyCode(btn) {
        const code = btn.previousElementSibling.innerText;
        navigator.clipboard.writeText(code);
        const originalText = btn.innerText;
        btn.innerText = 'Kopiert!';
        setTimeout(() => btn.innerText = originalText, 2000);
    }

    async function generateHash() {
        const text = document.getElementById('hash-input').value;
        const algo = document.getElementById('hash-algo').value;
        const output = document.getElementById('hash-output');

        if (!text) {
            output.innerText = '...';
            return;
        }

        try {
            const msgUint8 = new TextEncoder().encode(text);
            const hashBuffer = await crypto.subtle.digest(algo, msgUint8);
            const hashArray = Array.from(new Uint8Array(hashBuffer));
            const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
            output.innerText = hashHex;
        } catch (e) {
            output.innerText = "Fehler: " + e.message;
        }
    }

    function copyHash() {
        const hash = document.getElementById('hash-output').innerText;
        if (hash === '...') return;
        navigator.clipboard.writeText(hash);
        alert('Hash kopiert!');
    }

    function simulateReverse() {
        const name = document.getElementById('apk-name').value || "unknown.apk";
        const resultDiv = document.getElementById('reverse-result');
        const tree = document.getElementById('file-tree-container');
        
        resultDiv.style.display = 'block';
        document.getElementById('current-apk-label').innerText = name;
        tree.innerHTML = '<div class="loader"></div> Dekompiliere...';
        
        setTimeout(() => {
            tree.innerHTML = '';
            Object.keys(MOCK_FILES).forEach(file => {
                const div = document.createElement('div');
                div.className = 'file-item';
                div.innerHTML = `📄 ${file}`;
                div.onclick = () => {
                    const viewer = document.getElementById('file-content-viewer');
                    viewer.style.display = 'block';
                    viewer.innerText = MOCK_FILES[file];
                };
                tree.appendChild(div);
            });
            appData.analyses = (appData.analyses || 0) + 1;
            document.getElementById('analysis-count').innerText = appData.analyses;
            saveData();
        }, 1500);
    }

    function runDebugger() {
        const code = document.getElementById('debug-input').value;
        const results = document.getElementById('debug-results');
        results.innerHTML = '<div class="loader"></div> Analysiere Code-Struktur...';
        
        setTimeout(() => {
            let reports = [];
            if (code.includes('Activity') && !code.includes('onCreate')) reports.push('⚠️ Fehlende onCreate() Methode in Activity.');
            if (code.includes('INTERNET') && !code.includes('<uses-permission')) reports.push('❌ Berechtigung im Manifest-Format falsch.');
            if (code.length < 5) reports.push('ℹ️ Kein signifikanter Code zum Analysieren gefunden.');
            if (reports.length === 0) reports.push('✅ Keine offensichtlichen Syntax-Fehler gefunden.');
            
            results.innerHTML = reports.map(r => `<div class="card" style="border-left: 4px solid ${r.includes('✅') ? 'var(--primary)' : 'var(--danger)'}">${r}</div>`).join('');
        }, 1000);
    }

    function sendChat() {
        const input = document.getElementById('chat-input');
        const text = input.value.trim();
        if (!text) return;

        addChatMessage('user', text);
        input.value = '';

        setTimeout(() => {
            const response = generateAIResponse(text);
            addChatMessage('ai', response);
        }, 800);
    }

    function addChatMessage(role, text) {
        const box = document.getElementById('chat-box');
        const msg = document.createElement('div');
        msg.className = `msg ${role}`;
        msg.innerText = text;
        box.appendChild(msg);
        box.scrollTop = box.scrollHeight;
        
        appData.chatHistory.push({role, text});
        saveData();
    }

    function renderChatHistory() {
        const box = document.getElementById('chat-box');
        if (!appData.chatHistory) appData.chatHistory = [];
        appData.chatHistory.forEach(m => {
            const msg = document.createElement('div');
            msg.className = `msg ${m.role}`;
            msg.innerText = m.text;
            box.appendChild(msg);
        });
        box.scrollTop = box.scrollHeight;
    }

    function generateAIResponse(query) {
        query = query.toLowerCase();
        if (query.includes('hallo') || query.includes('hi')) return "Hallo! Ich bin bereit für Android-Coding. Was hast du vor?";
        if (query.includes('hash') || query.includes('sha')) return "Hashes sind wichtig für die Sicherheit. Du kannst meinen integrierten Hash-Generator im Menü nutzen, um SHA-256 oder SHA-512 Werte zu erzeugen.";
        if (query.includes('activity')) return "Um eine Activity zu erstellen, benötigst du eine Klasse, die von AppCompatActivity erbt und einen Eintrag im AndroidManifest.xml.";
        if (query.includes('intent')) return "Intents werden genutzt, um zwischen Komponenten zu kommunizieren. Beispiel: Intent(context, Target::class.java).";
        if (query.includes('permission') || query.includes('berechtigung')) return "Berechtigungen müssen im Manifest deklariert werden. Ab Android 6.0 müssen gefährliche Berechtigungen auch zur Laufzeit angefragt werden.";
        if (query.includes('smali')) return "Smali ist die Assembler-Sprache für die Dalvik VM. Sie ist essenziell für Reverse Engineering von APKs.";
        return "Das ist ein interessantes Thema. In der Android-Entwicklung nutzt man dafür meistens " + (query.length % 2 === 0 ? "Kotlin Coroutines" : "Jetpack Compose") + ". Soll ich dir ein Code-Beispiel zeigen?";
    }

    function initTimePicker() {
        const h = document.getElementById('hourSelect');
        const m = document.getElementById('minSelect');
        if (!h || !m) return;
        for(let i=0; i<24; i++) h.innerHTML += `<option value="${i}">${i.toString().padStart(2,'0')}</option>`;
        for(let i=0; i<60; i+=5) m.innerHTML += `<option value="${i}">${i.toString().padStart(2,'0')}</option>`;
    }

    function showTimePicker(input) {
        input.blur();
        const modal = document.getElementById("timePickerModal");
        modal.style.display = "flex";
        modal.dataset.targetInput = input.id;
    }

    function confirmTime() {
        const modal = document.getElementById("timePickerModal");
        const h = document.getElementById('hourSelect').value.padStart(2,'0');
        const m = document.getElementById('minSelect').value.padStart(2,'0');
        const inputId = modal.dataset.targetInput;
        if(inputId) document.getElementById(inputId).value = `${h}:${m}`;
        modal.style.display = "none";
        document.activeElement.blur();
    }

    document.body.addEventListener('dblclick', function(event) {
        event.preventDefault();
    });

    window.addEventListener('load', () => {
        loadData();
        renderDB();
        initTimePicker();
    });

    document.addEventListener("focusin", function(e) {
        if (e.target.tagName === "INPUT" && e.target.type !== "text") {
            setTimeout(() => e.target.blur(), 100);
        }
    });
</script>

</body>
</html>